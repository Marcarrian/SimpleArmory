import { ProgressFormatterPipe } from './progress-formatter.pipe';

describe('ProgressFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
